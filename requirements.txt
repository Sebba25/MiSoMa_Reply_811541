# Core runtime
pandas>=2.2
numpy>=1.26
pydantic>=2.10
pydantic-ai>=1.0.0
openai>=1.30

# Date parsing used by generated cleaners
python-dateutil>=2.9
dateparser>=1.2

# Environment + observability
python-dotenv>=1.0
logfire>=2.0

# App + notebook runtime
streamlit>=1.30
nest-asyncio>=1.6
ipython>=8.20
jupyter>=1.0
ipykernel>=6.29

# Optional notebook diagram support
# Note: the Python package is not enough by itself; Graphviz system binaries
# must also be installed and available on PATH to render diagrams.
graphviz>=0.20
